# Golang Iris Example. REST API CRUD for MongoDB.
For add new user by POST on /users/, write in Requset body JSON Object, as that:

```
{
    "Firstname": "First Name",
    "Lastname": "Last Name",
    "Age": int,
    "Msisdn": "Profile identificator code"
}
```